import { describe, it, expect } from 'vitest'
import * as Module from './navigation-tabs'

describe('NavigationTabs component', () => {
  it('module loads', () => {
    expect(Module).toBeTruthy()
  })
})
